<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoodMusic">

        <!-- Pantalla de inicio (Splash) - LAUNCHER principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MoodMusic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name=".InicioSesionActivity"
            android:exported="false"
            android:theme="@style/Theme.MoodMusic" />

        <!-- Registro -->
        <activity
            android:name=".RegistroActivity"
            android:exported="false"
            android:theme="@style/Theme.MoodMusic" />

        <!-- Estado de Ánimo -->
        <activity
            android:name=".EstadoAnimoActivity"
            android:exported="false"
            android:theme="@style/Theme.MoodMusic" />

        <!-- Registrar Estado -->
        <activity
            android:name=".RegistrarEstadoActivity"
            android:exported="false"
            android:theme="@style/Theme.MoodMusic" />

        <!-- Historial -->
        <activity
            android:name=".HistorialActivity"
            android:exported="false"
            android:theme="@style/Theme.MoodMusic" />

        <!-- Detalle Historial -->
        <activity
            android:name=".DetalleHistorialActivity"
            android:exported="false"
            android:theme="@style/Theme.MoodMusic" />

    </application>

</manifest>